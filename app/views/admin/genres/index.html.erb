admin ジャンル一覧、追加画面
<%= form_with model: @genre, url:admin_genres_path, local:true do |f| %>
<%= f.label :name %>
<%= f.text_area :name %>
<%= f.submit "新規登録" %>
<% end %>

<% @genres.each do |genres| %>
<%= genres.name %>
<% end %>